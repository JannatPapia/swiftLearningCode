import 'package:flutter/material.dart';

class One extends StatelessWidget {
 // const One({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightBlue,
      body: Center(
        child: Text("Star button"),
      ),
    );
  }
}
