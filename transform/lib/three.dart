import 'package:flutter/material.dart';

class Three extends StatelessWidget {
  // const One({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.teal,
      body: Center(
        child: Text("Lock your screen"),
      ),
    );
  }
}